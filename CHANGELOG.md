# 更新日志

## 1.0.0-alpha17 — 2026-09-03

### 体验优化

- 删除书籍增加二次确认：点击“删除”后弹出确认框，再次点击“确认删除”才会真正删除，避免误触丢失想保留的书籍。
- 定时停止分档改为 10 / 15 / 20 / 30 / 60 分钟，并新增“自定义分钟”（1–600 分钟）。
- 倍速新增“自定义倍速”入口，支持 0.5×–4× 的任意数值（保留原 0.75×–2.5× 快捷档）。

### 安装包与验证

- Android 版本升级为 `versionCode 17`、`versionName 1.0.0-alpha17`，可覆盖安装 alpha16 并保留本地书架与播放进度。

## 1.0.0-alpha16 — 2026-09-03

### 离线语音包管理

- 音色界面重构：移除此前意义不明的引擎音色列表（三个 zh 系统音色与“引擎默认音色”），界面底部只展示已下载的语音包。
- 语音包目录扩充为 7 个中文离线音色（男声/女声/多说话人/中英双语，50–176 MB），点击即直接下载并弹出安装，下载过程在界面内显示进度百分比。
- 已下载的语音包安装文件保留在本应用目录，可随时重新安装或手动删除；切换语音包、切换引擎或修改说话人时不再自动删除任何缓存。
- 自动识别当前安装的是哪个语音包（读取引擎 APK 内的模型目录），并以“当前使用 / 上次安装”标注。
- 朗读缓存键加入引擎安装时间戳与缓存纪元：语音包覆盖安装或引擎内更换说话人后自动重新合成，不再错误复用旧音频，也无需清空全部缓存。

### 下载与安装体验

- 语音包下载源改为 hf-mirror.com 公开镜像，改善大陆移动网络下的可达性。
- 下载失败时给出具体原因（存储空间不足、网络中断、服务器异常等）。
- 系统拦截安装时提示到设置中允许“安装未知应用”；手动重装使用 FileProvider 提供安装文件。

### 安装包与验证

- Android 版本升级为 `versionCode 16`、`versionName 1.0.0-alpha16`，可覆盖安装 alpha15 并保留本地书架与播放进度。
- debug 构建支持通过 `keystore.properties` 固定个人签名密钥，构建目录迁移后仍可覆盖安装。

### 已知构建环境问题

- 当前 Windows 工作目录含中文字符；本机 Gradle 的单元测试子进程会因路径编码问题无法加载测试类。应用源码和 APK 均已成功编译、打包。

## 1.0.0-alpha15 — 2026-09-02

### 网页小说正文清洗

- 新增针对网页头尾冗余内容的识别与移除，仅在章节边缘执行，避免误删正文中正常出现的词语。
- 过滤站点宣传、网盘合集、图书/分类导航等头部信息。
- 过滤连续 `====` / `＝＝＝` 等页面分隔符。
- 过滤“上一章 / 下一章 / 返回列表 / 返回目录”等分页和章节导航。
- 过滤经过斜杠拆分的“浏览器强制进入阅读模式”“退出转码阅读”等站点提示。
- 首次网页导入与后续自动加载章节共用同一套清洗规则。

### 安装包与验证

- Android 版本升级为 `versionCode 15`、`versionName 1.0.0-alpha15`，可覆盖安装 alpha14 并保留本地书架与播放进度。
- 已完成 debug APK 构建，并校验 APK 包名和版本信息。

## 1.0.0-alpha14

- 网页书籍添加时自动提取书名。
- 新增书架内重命名功能。
- 保持整章播放进度条和历史进度恢复能力。

## 1.0.0-alpha11

- 将播放器进度条改为按整个章节计算。
