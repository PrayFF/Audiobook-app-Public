# 更新日志

## 1.0.0-alpha24 — 2026-09-13

- 支持从 index.html 目录页直接导入完整章节列表，按目录顺序播放，无需正文页具有上下章链接。
- 当前章节及其后 10 章通过后台任务下载正文；换章和锁屏自动换章时更新范围，已有正文不重复下载。
- 固定预取范围，不再因已有缓存而继续向后扩展；已读章节保留以便回看，不自动删除。
- 网络恢复后重试下载；播放器显示后续正文缓存数量，提供“补齐缓存”按钮。人机验证需手动处理。
- 修复未缓存章节下载后无法开始播放的问题。缓存正文不等于预生成全部音频。

## 1.0.0-alpha23 — 2026-09-12

- 浏览器提取与后台章节下载共用 HTML 正文解析器，避免两套规则不一致。
- 排除评论列表、回复表单、分享、点赞、分类时间信息和相关推荐，再选择正文容器。
- 增加 article-content、post-content、articleBody 等正文容器优先级，不再按整个页面文字量选择正文。
- 浏览器提取时排除隐藏元素；保留小说段落及注释，不按“评论”等常用词删除小说句子。
- 已经混入正文的旧内容无法可靠还原 HTML 结构，请重新提取原网页。

## 1.0.0-alpha22 — 2026-09-12

- 网页正文优先识别无忧书城实际使用的 `#neirong` 等容器，排除评论和广告；保留段落换行。
- 修复后台抓取先删除导航导致下一页链接丢失；支持 BookNext、带空格的下一页标签、无效链接跳过。
- 已导入但缺失下一章链接的网页书籍，在点击下一章时重新检查来源；目录占位章节下载后保存下一章链接。
- 粘贴网址时清除末尾中文标点；临时连接失败最多延迟重试两次，取消未经证实的 DNS 污染结论。
- 人机验证由用户在网页内手动完成，遇到验证页停止正文导入。未实现自动点击验证码。
- 连接与站点访问限制仍依赖手机网络和网站；重试无法保证拒绝连接的站点恢复。

## 1.0.0-alpha21 — 2026-09-04

### 网页导入修复

- 修复短章节正文被误删的问题：部分站点在正文前插入「上一章/下一章：第 X 章」导航行和反爬提示（如「退出转码阅读」），此前会被误判为页脚而把整篇正文截掉，导致「清理导航信息后没有识别到足够正文」。现在能正确跳过这类导航行并保留正文。
- 网页打开失败时，对「连接被拒绝」给出更准确的提示：明确指出可能是域名被污染（解析到无效地址），并建议更换 DNS 或使用代理。

### 安装包与验证

- Android 版本升级为 `versionCode 21`、`versionName 1.0.0-alpha21`，可覆盖安装 alpha20 并保留书架与播放进度。

## 1.0.0-alpha20 — 2026-09-04

### 书架与收藏夹

- 收藏夹「管理」按钮固定在最右侧，新建收藏夹后不再随筛选项增多而右移。
- 新增长按拖拽排序：长按书籍约 1 秒后上下拖拽，直接插入到任意两本书之间调整顺序；拖拽经过两本书中间时显示插入指示线，松手即完成排序。

### 网页导入与预下载

- 新书提取到书架后，默认预下载后续 10 章正文；随着听书章节推进，始终保持已下载最新 10 章的滑动窗口，避免阅读时逐章等待下载。
- 正文提取增加人机验证（Cloudflare 挑战页）检测：站点在做验证时会给出明确提示，不再误报「未识别到足够正文」。
- 网页打开失败的提示更准确：区分域名解析失败（可能被屏蔽）、连接被拒、连接超时等不同原因。

### 语音包与音色

- 语音包列表按加载速度重排，快速模型（matcha/piper，加载与合成快约 4 倍）排前并标注「推荐·最快」，慢速大模型标注「加载较慢/最慢」。
- 修复音色切换时的重复加载：仅在音色真正变化时才重新设置引擎音色，显著减少两段文字之间的卡顿。

### 安装包与验证

- Android 版本升级为 `versionCode 20`、`versionName 1.0.0-alpha20`，可覆盖安装 alpha19 并保留书架与播放进度。

## 1.0.0-alpha19 — 2026-09-04

### 恢复默认章节名

- 目录编辑模式中，已改名的章节可点击“恢复”回到网页原始章节名。
- 支持一键“恢复全部默认名”，撤销所有手动与批量改名。

### 收藏夹

- 书架顶部新增收藏夹分组：可新建收藏夹、重命名、删除（删除仅解散分组，不删书）。
- 书籍 ⋯ 菜单支持“移入收藏夹”，也可随时移出到“未分组”。
- 书架可按收藏夹筛选浏览。

### 书架管理

- 书籍 ⋯ 菜单整合：移入收藏夹、上移、下移、重命名、删除（保留二次确认）。
- 书架排版可在“列表”与“九宫格”之间切换，选择持久保存。

### 全书听书进度

- 听书页顶部显示整本书进度：当前章节 / 总章数 + 百分比进度条（按章节位置 + 本章内进度估算）。

### 安装包与验证

- Android 版本升级为 `versionCode 19`、`versionName 1.0.0-alpha19`，数据库自动迁移（v1→v2，新增收藏夹表与章节默认名字段），覆盖安装保留书架与播放进度。

## 1.0.0-alpha18 — 2026-09-04

### 目录功能

- 从网页章节页新增书籍时，自动识别页面里的“目录 / 章节目录”链接并抓取目录页，解析出全书章节标题与链接，作为目录保存。
- 只保存目录（章节标题 + 链接），不下载全部章节正文；首次进入仅下载当前打开的章节，其余章节在点击时才按需抓取。
- 播放器顶部章节下拉升级为完整目录：可看到全部章节，未下载的章节会标注“未下载，点击后加载”，点击即跳转并抓取对应章节。
- 目录未识别到或抓取失败时，自动回退为原来的单章导入方式，不影响既有流程。

### 章节名编辑

- 目录支持编辑模式：点击单个章节可手动改名。
- 批量改名三种方式：去掉固定字段（例如书名前缀或“第XX章”字样）、去掉所有章节开头 N 个字、去掉所有章节结尾 N 个字；批量操作前有示例预览。

### 安装包与验证

- Android 版本升级为 `versionCode 18`、`versionName 1.0.0-alpha18`，可覆盖安装 alpha17 并保留本地书架与播放进度。

## 1.0.0-alpha17 — 2026-09-03

### 体验优化

- 删除书籍增加二次确认：点击“删除”后弹出确认框，再次点击“确认删除”才会真正删除，避免误触丢失想保留的书籍。
- 定时停止分档改为 10 / 15 / 20 / 30 / 60 分钟，并新增“自定义分钟”（1–600 分钟）。
- 倍速新增“自定义倍速”入口，支持 0.5×–4× 的任意数值（保留原 0.75×–2.5× 快捷档）。

### 安装包与验证

- Android 版本升级为 `versionCode 17`、`versionName 1.0.0-alpha17`，可覆盖安装 alpha16 并保留本地书架与播放进度。

## 1.0.0-alpha16 — 2026-09-03

### 离线语音包管理

- 音色界面重构：移除此前意义不明的引擎音色列表（三个 zh 系统音色与“引擎默认音色”），界面底部只展示已下载的语音包。
- 语音包目录扩充为 7 个中文离线音色（男声/女声/多说话人/中英双语，50–176 MB），点击即直接下载并弹出安装，下载过程在界面内显示进度百分比。
- 已下载的语音包安装文件保留在本应用目录，可随时重新安装或手动删除；切换语音包、切换引擎或修改说话人时不再自动删除任何缓存。
- 自动识别当前安装的是哪个语音包（读取引擎 APK 内的模型目录），并以“当前使用 / 上次安装”标注。
- 朗读缓存键加入引擎安装时间戳与缓存纪元：语音包覆盖安装或引擎内更换说话人后自动重新合成，不再错误复用旧音频，也无需清空全部缓存。

### 下载与安装体验

- 语音包下载源改为 hf-mirror.com 公开镜像，改善大陆移动网络下的可达性。
- 下载失败时给出具体原因（存储空间不足、网络中断、服务器异常等）。
- 系统拦截安装时提示到设置中允许“安装未知应用”；手动重装使用 FileProvider 提供安装文件。

### 安装包与验证

- Android 版本升级为 `versionCode 16`、`versionName 1.0.0-alpha16`，可覆盖安装 alpha15 并保留本地书架与播放进度。
- debug 构建支持通过 `keystore.properties` 固定个人签名密钥，构建目录迁移后仍可覆盖安装。

### 已知构建环境问题

- 当前 Windows 工作目录含中文字符；本机 Gradle 的单元测试子进程会因路径编码问题无法加载测试类。应用源码和 APK 均已成功编译、打包。

## 1.0.0-alpha15 — 2026-09-02

### 网页小说正文清洗

- 新增针对网页头尾冗余内容的识别与移除，仅在章节边缘执行，避免误删正文中正常出现的词语。
- 过滤站点宣传、网盘合集、图书/分类导航等头部信息。
- 过滤连续 `====` / `＝＝＝` 等页面分隔符。
- 过滤“上一章 / 下一章 / 返回列表 / 返回目录”等分页和章节导航。
- 过滤经过斜杠拆分的“浏览器强制进入阅读模式”“退出转码阅读”等站点提示。
- 首次网页导入与后续自动加载章节共用同一套清洗规则。

### 安装包与验证

- Android 版本升级为 `versionCode 15`、`versionName 1.0.0-alpha15`，可覆盖安装 alpha14 并保留本地书架与播放进度。
- 已完成 debug APK 构建，并校验 APK 包名和版本信息。

## 1.0.0-alpha14

- 网页书籍添加时自动提取书名。
- 新增书架内重命名功能。
- 保持整章播放进度条和历史进度恢复能力。

## 1.0.0-alpha11

- 将播放器进度条改为按整个章节计算。
